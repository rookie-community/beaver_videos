<script type="text/html" id="toolCreated">
    <span>
        {{#  if(d.created != null){  }}
        {{= new Date(d.created).toLocaleString()}}
        {{#  } }}
    </span>
</script>
<script type="text/html" id="toolUpdated">
    <span>
        {{#  if(d.updated != null){  }}
        {{= new Date(d.updated).toLocaleString()}}
        {{#  } }}
    </span>
</script>
