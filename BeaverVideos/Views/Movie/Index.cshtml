@using MoviesLibrary.Common.Enum
@using MoviesLibrary.Model
@using System.Text.Json
@model IEnumerable<TopModel>
@{
    var routeData = new Dictionary<string, string>();
}
<div class="layui-container">
    <div class="layui-row" style="margin-top:50px;margin-bottom:20px;">
        <h1 class="layui-col-md-offset5 layui-col-md2">小狸影视</h1>
    </div>
    <div class="layui-row">
        <form class="layui-form" asp-controller="Movie" asp-action="Search" method="get">
            @*@Html.AntiForgeryToken()*@
            <div class="layui-col-md10">
                <input type="text" name="name" autocomplete="off" placeholder="请输入影视名称或视频链接地址" class="layui-input">
            </div>
            <div class="layui-col-md2">
                <button class="layui-btn layui-btn-normal layui-btn-fluid" type="submit" target="_blank">查询</button>
            </div>
        </form>
    </div>
    <blockquote class="layui-elem-quote" style="margin-top:20px;">排行榜</blockquote>
    <div class="layui-row">
        <div class="layui-tab layui-tab-brief" lay-filter="docDemoTabBrief">
            <ul class="layui-tab-title">
                <li class="layui-this">电影</li>
                <li>电视剧</li>
                <li>综艺</li>
                <li>动漫</li>
            </ul>
            <div class="layui-tab-content" style="height: 100px;">
                @foreach (var tops in Model.GroupBy(x => x.Cat).ToList())
                {
                    <div class="layui-tab-item layui-show">
                        <div class="layui-row">
                            @foreach (var item in tops)
                            {
                                routeData = new Dictionary<string, string>();
                                routeData.Add("entId", item.EntId);
                                routeData.Add("catType", ((int)item.Cat).ToString());

                                <div class="layui-col-md2 layui-col-sm4">
                                    <div class="layui-panel" style="width:183px;height:300px;margin:5px;">
                                        <a asp-controller="Movie" asp-action="Detail" asp-all-route-data="routeData" target="_blank">
                                            <img src="@item.Cover" style="width:183px;height:257px;" />
                                            <label class="layui-form-label" style="width:150px;text-align:center">@item.Title</label>
                                        </a>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>